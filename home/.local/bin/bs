#!/usr/bin/env bash
set -euo pipefail

# Executes a command in the background and silences its output.
# Source: https://codeberg.org/EvanHahn/dotfiles

if test -t 1; then
  exec 1>/dev/null
fi

if test -t 2; then
  exec 2>/dev/null
fi

"$@" &
